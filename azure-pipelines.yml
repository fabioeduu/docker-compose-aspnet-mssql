trigger:
- main

pool:
  vmImage: ´ubuntu-latest´

- task: Docker@2
  inputs:
    containerRegistry: 'dotnetmysqlrm560416'
    repository: 'dotnetsql'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: |
      $(Build.BuildId)
      latest
